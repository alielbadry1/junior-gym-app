# Junior Gym — ملف تسليم فني كامل (لـ Claude Code / أي مبرمج يكمل المشروع)

> اقرأ الملف ده كامل قبل ما تعدّل أي حاجة. فيه: السياق التجاري، القرارات المعمارية، إيه اللي اتعمل فعليًا، إيه اللي لسه، وإيه اللي غير مؤكد ومحتاج تحقق.

---

## 1. السياق التجاري (Business Context)

**العميل:** Junior Gym — أكاديمية رياضية وأنشطة للأطفال في دمياط (Captain Ahmed Shousha)، فرع واحد فقط.

**الهدف:** بناء تطبيق ويب متكامل (محاسبي + مالي + إداري) يحل محل نظام إكسيل يدوي عمره ~سنة، فيه ~25 ألف سجل، مقسّم على 6 ملفات إكسيل بسبب تعقيدات الصلاحيات.

**المستخدمون المستهدفون (لسه مفيش نظام صلاحيات مبني):**
- مالك المؤسسة (Owner)
- مدير الحسابات (Accounts Manager)
- محاسب مكتب (Office Accountant)
- 4 سكرتارية بالجيم على شيفتين — هم مصدر البيانات الخام (حضور، أنشطة، اشتراكات، إيصالات، مصروفات نثرية)

**متطلبات الصلاحيات (لم تُبنَ بعد):** لكل مستخدم — أي شاشات يقدر يدخلها + صلاحيات تعديل/حذف مستقلة داخل كل شاشة.

**قيود مهمة:**
- المستخدم الأساسي (مدير المكتب) من غير خلفية برمجية — أي تواصل معاه لازم يكون مبسّط جدًا وبخطوات تفصيلية.
- كل البيانات التاريخية في الإكسيل القديم لازم تتهاجر بالكامل — دي بيانات عملاء حرجة.
- الجزء المحاسبي والإداري مطلوب يتبنيا مع بعض من البداية لأنهم متشابكين بالكامل (مش مرحلتين منفصلتين).

---

## 2. الـ Stack التقني (مُقرَّر ومُنفَّذ)

- **Frontend/Backend:** Next.js 16 (App Router, Turbopack) + TypeScript + React 19
- **Styling:** Tailwind CSS v4 (نظام CSS-based config عبر `@theme` جوه `app/globals.css`، مفيش `tailwind.config.js`)
- **Database:** Supabase (Postgres) — خطة Free
- **Hosting:** Vercel (Free) — لسه مش متربط بالـ repo فعليًا
- **Auth/RLS:** لسه مفيش نظام مستخدمين ولا Row Level Security مُفعّل — الاتصال حاليًا عن طريق الـ anon/publishable key بدون قيود. **ده لازم يتعالج قبل أي نشر production حقيقي.**

### بيانات الاتصال
```
NEXT_PUBLIC_SUPABASE_URL=https://outloitmkhlgzlglvssw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ms51rAMvNExp63B3X2A6NQ_K3QMUAhJ
```
(القيم دي "publishable" بتصميمها — آمن تكون في كود الواجهة الأمامية. الـ secret key بتاع Supabase **لم يُشارك ولا يجب أن يُشارك** مع أي طرف تاني أو يُستخدم في كود client-side.)

- **GitHub repo:** `github.com/alielbadry1/junior-gym-app` (اتعمل فاضي، لسه محتاج push أول مرة — الرفع المباشر من بيئة Claude السحابية كان محجوب أمنيًا، فالكود اتسلّم كـ zip للمستخدم عشان يرفعه بنفسه عبر GitHub Desktop. لو لسه معملش push، ده أول حاجة تتعمل).

---

## 3. Database Schema (كامل، مستخرج فعليًا من القاعدة الحية بتاريخ 2026-08-13)

انظر أيضًا `types/database.ts` في الكود — فيه الأنواع الكاملة + الـ Relationships بين الجداول.

الجداول الموجودة فعليًا في schema `public`:
`activities, cash_accounts, cost_centers, daily_transactions, departments, financial_statements, fixed_assets, journal_entries, main_accounts, parties, partners, party_roles, program_trainer_assignments, programs, sub_accounts, subscription_sessions, subscriptions, transaction_types`

**النموذج الأساسي (Core Data Model):**
- **`parties`** = كيان موحّد لكل الأشخاص (عملاء، مدربين، موظفين...). الشخص ممكن يكون له أكتر من دور في نفس الوقت (مثال حقيقي: "رواء حامد" معلمة حضانة براتب ثابت + مدربة بنظام نسبة).
- **`party_roles`** = الأدوار المرتبطة بكل شخص (عمود `role` هو **DB enum غير مؤكد القيم** — انظر قسم "غير مؤكد" تحت).
- **`programs`** = نشاط + عدد سيشن + سعر (شبه ثابت). مرتبط بـ `activities`.
- **`program_trainer_assignments`** = تكليف مدرب/مدربين لبرنامج بنسبة معينة، **مؤرشف بالتاريخ** (starts_at/ends_at) — يعني بيتغير مع الوقت وكل نسخة بتفضل محفوظة.
- **`subscriptions`** = المحور الأساسي. يربط طالب واحد (`customer_party_id`) ببرنامج واحد، بعدد سيشنات وسعر. الطالب ممكن يملك أكواد اشتراك متعددة متزامنة. عمود `trainer_assignment_snapshot` (jsonb) بياخد **نسخة (snapshot)** من تكليف المدرب الفعّال وقت إنشاء الاشتراك، حتى لو النسبة اتغيرت بعدين في `program_trainer_assignments`.
- **`subscription_sessions`** = شبكة السيشنات لكل اشتراك (رقم/تاريخ متوقع/تاريخ فعلي/حالة: على الأرجح `pending`/`attended`/`absent`/`excused` بناءً على المتطلبات، **لكن القيم دي نص حر `text` مش enum فمفيش قيد على القيم في الداتابيز نفسها**).

**الجانب المحاسبي:**
- `financial_statements` ← `main_accounts` ← `sub_accounts` (شجرة حسابات 3 مستويات)
- `cash_accounts` = حسابات الخزينة (منفصلة شكليًا عن `sub_accounts` — **مفيش FK مباشر بينهم في الـ schema الحالي**، ده نقطة تحتاج انتباه لو هتبني تقرير رصيد الخزينة).
- `transaction_types` = دليل أنواع العمليات (~35 نوع متفق عليهم في التصميم، **لكن الجدول نفسه فاضي دلوقتي - لسه محدش زرعه بالبيانات**). فيه `debit_sub_account_id` / `credit_sub_account_id` / `generates_entry` / `triggers_cogs`.
- `daily_transactions` = العملية اليومية الفعلية (مرتبطة بـ transaction_type, subscription, customer, program, cash_account...).
- `journal_entries` = القيد المحاسبي الفعلي (مدين/دائن على sub_accounts)، مع `is_cogs_entry` و `trainer_party_id` لدعم قاعدة الـ COGS التلقائية.
- `cost_centers` = مركزي تكلفة: **الجيم** و **لاند** (مبنى الحضانة) — مستقلان تمامًا.
- `partners` = شركاء لاند (3 شركاء حاليًا، Captain Ahmed بأكبر نسبة + شريكان)، لكل شريك `share_percent`.
- `fixed_assets` = الأصول الثابتة مع إهلاك شهري متراكم.

**قواعد محاسبية متفق عليها (لسه مش مبنية في الكود — دي تصميم فقط):**
1. "تسجيل الاشتراك" نفسه **بلا قيد محاسبي** (مجرد التزام تعاقدي).
2. "حضور نشاط" / "غياب محتسب" → مدين العملاء / دائن المبيعات (الإيراد يتحقق لحظة الحضور مش وقت الاشتراك) — **إلا** الحضانة والرحلات اللي بتتسجل فورًا عند الاشتراك.
3. "استلام نقدي" → مدين النقدية / دائن العملاء.
4. **قاعدة COGS تلقائية:** أي قيد دائنه "المبيعات" على نشاط له مدرب نسبة → يولّد تلقائيًا قيد مرافق: مدين "رواتب المدربين (COGS)" / دائن "الرواتب المستحقة". حاليًا حساب موحّد واحد، **مقرر لاحقًا** يتقسم لحسابين (مدربين % / موظفين راتب ثابت).
5. "إيقاف اشتراك" و"استرداد الاشتراك" **دائمًا متساويين بالقيمة**؛ أي فرق (بسبب خصم سابق) بيتسوّى بقيد "تسوية حساب".
6. "غياب غير محتسب": بلا حد أقصى لعدد المرات، لكن لازم حقل سبب/ملاحظة إجباري.
7. المدرب/الموظف ممكن يكون له رصيد سالب مؤقت (سلفة) — مسموح به عمدًا.
8. **الخزينة يجب تُمنع من الوصول لرصيد سالب** — قيد صارم لازم يتطبق في منطق التطبيق (مش موجود حاليًا كـ DB constraint).

**قاعدة نشاط/توقف العميل (Client Active/Inactive — نهائية، لسه مش مطبقة):**
- نشط إذا: عليه مديونية (تجاري أو محاسبي) — أو — له رصيد — أو — آخر حضور خلال آخر شهر.
- يتحول متوقف تلقائيًا بعد شهر كامل بدون أي رصيد وبدون حضور جديد.

**شاشة قائمة الدخل الموحّدة (تصميم متفق عليه، مش مبني بعد):** شاشة ديناميكية واحدة بدل 5 تقارير، بـ 3 عناصر تحكم: Group By (قسم ▸ نشاط ▸ برنامج)، فترة زمنية قابلة للتقسيم لأعمدة مقارنة، ونطاق مركز التكلفة (جيم/لاند/مجمّع). توزيع المصروفات غير المباشرة تلقائي بنسبة الإيراد.

---

## 4. إيه اللي اتبنى فعليًا في الكود (2026-08-13)

- ✅ Scaffold: Next.js 16 + TypeScript + Tailwind v4 + `@supabase/ssr` (`lib/supabase/client.ts` للـ browser، `lib/supabase/server.ts` للـ server components — بيستخدم async `cookies()` زي ما Next 16 بيتطلب)
- ✅ `types/database.ts` — أنواع TypeScript كاملة مطابقة للـ schema الحي، شاملة `Relationships` عشان الـ embedded selects (زي `party_roles(role,status)`) تتايب صح. **الـ `foreignKeyName` strings synthetic مش من constraints حقيقية — القيم شغالة فعليًا لكن الاسم نفسه تخميني.**
- ✅ هوية بصرية: تيل غامق + كورال/أورانج + أمبر، عرّفت كـ CSS variables في `app/globals.css` عبر `@theme inline` (Tailwind v4 syntax). خط Cairo محمّل عبر `<link>` تاج في `app/layout.tsx` (**مش `next/font/google`** — كان بيفشل وقت البناء لو مفيش اتصال بالإنترنت لـ fonts.googleapis.com وقت الـ build، فاستخدمنا `<link>` عادي بدل منه عشان يفضل مستقل عن الشبكة وقت البناء).
- ✅ `dir="rtl" lang="ar"` على مستوى `<html>` بالكامل.
- ✅ `components/AppShell.tsx` — sidebar + layout عام لكل الشاشات.
- ✅ **شاشة Dashboard** (`app/page.tsx`) — 4 كروت إحصائية:
  - عملاء نشطون: `count` من `subscriptions` حيث `status = 'active'` (تقريب مبسّط، مش القاعدة النهائية لـ active/inactive المذكورة فوق)
  - تحصيلات اليوم: `sum(amount)` من `daily_transactions` مربوطة بـ `transaction_types` حيث `name ILIKE '%استلام%'` وتاريخ اليوم (**هترجع صفر دلوقتي لأن `transaction_types` جدول فاضي**)
  - حضور اليوم: `count` من `subscription_sessions` حيث `actual_date = اليوم` و `status = 'attended'`
  - رصيد الخزينة: **placeholder ثابت "قيد الإعداد"** — عمدًا، لأن `cash_accounts` مش مربوطة بـ `sub_accounts` بشكل مباشر في الـ schema، فمحرك حساب الرصيد محتاج تصميم إضافي قبل ما يتبنى صح.
- ✅ **شاشة الطلاب** (`app/students/*`) — كاملة CRUD:
  - `app/students/page.tsx`: عرض + بحث (بالاسم/الهاتف) + عرض الأدوار كـ tags
  - `app/students/new/page.tsx` + `app/students/actions.ts` (`createStudent`): إنشاء `party` جديد + `party_role` بدور `customer` تلقائيًا
  - `app/students/[id]/edit/page.tsx` + `updateStudent`: تعديل بيانات الطالب
- ✅ تحقق: `npx tsc --noEmit` نظيف، `npx eslint .` نظيف، `npm run build` ناجح، وتشغيل فعلي (`next start`) بيرجع 200 من غير كراش على الصفحات المبنية.
- ✅ **شاشة المدربين** (`app/trainers/*`) — عرض + إضافة مدرب (party جديد بدور `trainer`).
- ✅ **شاشة إعدادات البرامج** (`app/programs/setup`) — تهيئة مراكز التكلفة (الجيم/لاند)، إضافة أقسام رئيسية وأنشطة.
- ✅ **شاشة الأنشطة والبرامج** (`app/programs`, `app/programs/new`, `app/programs/[id]/edit`) — عرض مجمّع حسب القسم، إنشاء/تعديل برنامج مع تكليف مدرب أو أكتر ونسبهم، اسم البرنامج يتولّد تلقائيًا [النشاط]/[عدد السيشن]/[المدرب الرئيسي]، ومنطق `syncTrainerAssignments` بيطبّق الأرشفة الزمنية (Versioned) عند تغيير نسبة أو مدرب.
- ✅ **شاشة الاشتراكات** (`app/subscriptions/*`) — إنشاء اشتراك مع snapshot تكليف المدرب وتوليد تلقائي لـ `subscription_sessions` حسب أيام البرنامج، قائمة الاشتراكات، وشاشة تفاصيل بمتتبع سيشنات مرئي (مربعات ملوّنة).
- ✅ **محرك القيود المحاسبية** (`lib/accounting.ts`) — `recordTransaction()` بيولّد `journal_entries` تلقائيًا من `transaction_types`، شامل قاعدة COGS التلقائية (نسبة المدرب من المبلغ المحصَّل فعليًا) ومنع أي عملية تخلي رصيد حساب نقدي سالب.
- ✅ **شجرة حسابات أولية + أنواع العمليات** (`app/accounting/setup`) — زرع idempotent لـ `financial_statements`/`main_accounts`/`sub_accounts`/`cash_accounts`/`transaction_types` (~45 نوع) مبني على أسماء الحسابات المذكورة في `CLAUDE_PROJECT_BRIEF.md`. **أولي، محتاج مراجعة صاحب المشروع قبل الترحيل.**
- ✅ **شاشة الحضور اليومي** (`app/attendance/*`) — بحث بكود الاشتراك، تسجيل حضور/غياب محتسب (بيسجّل إيراد)/غياب غير محتسب (بدون قيد، سبب إجباري)، مع خيار تحصيل نقدي متزامن.

**فجوة schema محتاجة migration:** عمود `subscription_sessions.excused_reason` مذكور في `schema.sql` لكن غير موجود فعليًا في القاعدة الحية (تأكد غيابه من `DATABASE_SCHEMA.md`). الحل المؤقت: السبب بيتسجّل في `daily_transactions.notes`. شغّل `alter table subscription_sessions add column excused_reason text;` في Supabase SQL Editor لحل الفجوة نهائيًا، وبعدها انقل منطق الحفظ من `notes` للعمود.

تفاصيل كاملة لكل ما سبق في `PROGRESS_LOG.md` بتاريخ 2026-08-14 (الدفعة الثانية والثالثة).

**ملاحظة مهمة:** الملف المرجعي الأدق دلوقتي هو `CLAUDE_PROJECT_BRIEF.md` (تسليم أشمل موجود في نفس المجلد، ومعاه `schema.sql` الأصلي ومجلد `مرفقات_مرجعية/`) — راجعه هو أولًا لأي تفصيلة محاسبية دقيقة، وارجع لمرفقات هذا المجلد (خصوصًا `البرامج_ونسب_المدربين.pdf` و`الخريطة_الذهنية_لجونيور_جيم.pdf`) قبل ما تسأل صاحب المشروع سؤال ممكن يكون اتجاوب عليه فعلاً هناك.

---

## 5. غير مؤكد ومحتاج تحقق قبل ما تكمل (⚠️ مهم)

1. ~~**`party_roles.role` enum:**~~ **✅ محسوم.** تأكد من `schema.sql` الأصلي (`create type party_role as enum ('customer','trainer','employee','owner','partner');`) — القيمة `"customer"` المستخدمة في `app/students/actions.ts` صحيحة. مسميات وظيفية زي مدير حسابات/محاسب مكتب/سكرتارية **مش** قيم enum، هتتبنى لاحقًا في نظام صلاحيات مستقل (جدول مستخدمين منفصل، مش `party_roles.role`).
2. **`cash_accounts` ↔ `sub_accounts`:** مفيش عمود ربط مباشر بينهم في الـ schema الحالي. لازم تتأكد إزاي هيتحسب "رصيد الخزينة" فعليًا — يمكن محتاج migration تضيف `sub_account_id` على `cash_accounts`، أو منطق مختلف تمامًا.
3. **`transaction_types` جدول فاضي:** الـ ~35 نوع عملية المتفق عليهم (تصميميًا) لسه ما اتزرعوش كبيانات في الجدول. لازم seed script قبل ما أي شاشة تسجيل عمليات تشتغل صح.
4. **RLS/Auth:** مفيش نظام مستخدمين ولا Row Level Security. أي حد معاه الـ anon key (اللي هو public بالتصميم) يقدر يقرأ/يكتب في كل الجداول حاليًا. **لازم يتبنى قبل أي استخدام حقيقي ببيانات عملاء حساسة.**
5. **الـ `foreignKeyName` في `types/database.ts`:** أسماء تخمينية (زي `daily_transactions_transaction_type_id_fkey`) مش مسحوبة من constraints حقيقية — بتشتغل صح للـ type-checking لكن لو حد يعتمد عليها runtime (نادر) يتأكد الأول.

---

## 6. الخطوات القادمة (بالترتيب المقترح)

1. ~~**[مستخدم] رفع الكود على GitHub**~~ ✅ تم (`origin/main` مطابق لـ `main` المحلي).
2. **[معوّق — محتاج المستخدم] ربط GitHub بـ Vercel** — استيراد الـ repo، إضافة الـ environment variables (نفس القيم في قسم 3 فوق)، Deploy. إجراء خارجي (تسجيل دخول Vercel) ما ينفعش يتعمل من هنا.
3. ~~**تأكيد enum قيم `party_roles.role`**~~ ✅ تم، محسوم من `schema.sql` (انظر قسم 5.1).
4. ~~**بناء شاشة البرامج والأنشطة**~~ ✅ تم (`app/programs/*`, `app/trainers/*`).
5. ~~**بناء شاشة الاشتراكات**~~ ✅ تم (`app/subscriptions/*`).
6. ~~**Seed `transaction_types`**~~ ✅ تم (`app/accounting/setup`)، وكمان شجرة الحسابات كاملة والحسابات النقدية.
7. ~~**بناء محرك القيود المحاسبية**~~ ✅ تم (`lib/accounting.ts`) — COGS تلقائي + منع رصيد الخزينة السالب.
8. ~~**بناء شاشة الحضور اليومي**~~ ✅ تم (`app/attendance/*`) — متصلة بمحرك القيود.
9. ~~**شاشة إيقاف/استرداد الاشتراك**~~ ✅ تم (`app/subscriptions/[id]/stop`) — قيد "تسوية حساب" التلقائي بالتفصيل سيبناه خطوة يدوية عمدًا (بند مؤجل رسميًا، شوف `PROGRESS_LOG.md` 2026-08-18).
10. ~~**قائمة الدخل الموحّدة وباقي التقارير**~~ ✅ تم (`app/reports/*`) — كشوف حساب عملاء/مدربين/موظفين، النقدية، قائمة الدخل الموحّدة، ميزان المراجعة، المركز المالي، الالتزامات المستحقة. تفاصيل التبسيطات المتعمّدة في `PROGRESS_LOG.md` 2026-08-19.
11. **[نص مبني، مش مفعّل] RLS + نظام صلاحيات المستخدمين** — تسجيل الدخول (`/login`, `/signup`, `/users`) شغّال، بس القفل الفعلي (RLS + `AUTH_ENFORCED=true`) مطفي عمدًا لحد ما صاحب المشروع يجهّز حسابات الـ8 أشخاص. خطوات التفعيل كاملة في `SETUP_AUTH.md`. صلاحيات الشاشات بالتفصيل (تعديل/حذف لكل يوزر لوحده) لسه المرحلة الجاية.
12. **[معوّق — محتاج ملفات من المستخدم] ترحيل بيانات الإكسيل القديمة** (~25,000 سجل) — الملفات نفسها مش موجودة في المشروع؛ لازم تُرفع الأول، وبعدين تصميم استراتيجية الترحيل + مراجعة الأرصدة الافتتاحية الشاذة. `app/*/import` (استيراد إكسيل) جاهزة كأداة تنفيذ لما الملفات توصل.
13. **[معوّق — محتاج عقد من كابتن أحمد] هيكلة حسابات شركاء لاند وتوزيع الأرباح** — حساب "جاري شريك (لاند)" في شجرة الحسابات مؤقت عام موحّد لحد ما يترفع العقد.

---

## 7. نقاط مؤجلة (من ملف "لاحقًا" الأصلي، لسه محتاجة قرار/تصميم)

- حساب التسوية التلقائي بالتفصيل
- تقرير حساب العملاء بمستويين (إجمالي/تفصيلي بالنشاط)
- فصل حساب "الرواتب المستحقة" لحسابين (مدربين/موظفين)
- مراجعة الأرصدة الافتتاحية الشاذة عند الترحيل
- شاشة التزامات مستقلة
- شاشة صلاحيات وشركاء لاند (آلية توزيع الربح التفصيلية من عقد لسه ما اترفعش)

---

*آخر تحديث: 2026-08-14. حدّث الملف ده لما تخلص أي بند من قسم "الخطوات القادمة" فوق، عشان أي حد (إنسان أو Claude) يقدر يكمل من نفس النقطة.*
