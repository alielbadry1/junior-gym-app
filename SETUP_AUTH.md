# تفعيل تسجيل الدخول والمستخدمين — خطوة بخطوة

هذا الملف موجّه لصاحب المشروع (كابتن أحمد) مباشرة. اتبع الخطوات بالترتيب.

---

## 1) شغّل ملف الهجرة الأول (آمن، مش هيقفل حاجة)

1. افتح [supabase.com/dashboard](https://supabase.com/dashboard) ← اختر مشروع "Junior Gym".
2. من القائمة الجانبية: **SQL Editor** ← **New query**.
3. افتح ملف `migrations/002_auth_and_permissions.sql` من مجلد المشروع، انسخ محتواه كامل، الصقه في المحرر.
4. دوس **Run**. المفروض تشوف رسالة نجاح.

هذا الملف بيضيف جدولين جداد بس (`app_users`, `app_user_permissions`) — **مش بيأثر على أي جدول موجود ولا بيقفل حاجة**.

## 2) تأكد إن تأكيد الإيميل مطفي (اختياري بس بيسهّل عليك)

من نفس الداشبورد: **Authentication** ← **Providers** ← **Email** ← تأكد إن "Confirm email" مطفية (Off). لو مفعّلة، أول تسجيل هيبعتلك رابط تأكيد في الإيميل قبل ما تقدر تدخل — مش خطأ، بس خطوة زيادة.

## 3) سجّل أول حساب (هيبقى تلقائيًا "مالك")

1. افتح التطبيق على `/signup` (مثلاً `https://your-app.vercel.app/signup`).
2. اكتب اسمك، إيميلك، وكلمة سر.
3. أول حساب يتسجّل في النظام كله بيتحول تلقائيًا لحساب **مالك (Owner)** ومفعّل على طول.

## 4) اعتماد باقي المستخدمين

كل حساب تاني (مدير حسابات، محاسب مكتب، السكرتارية الأربعة) لازم:
1. يسجّل حساب بنفسه من `/signup` (أو تعمله أنت له من **Authentication ← Users ← Add user** في نفس داشبورد Supabase).
2. حسابه هيفضل "معلّق" لحد ما تدخل إنت (بحساب المالك) على شاشة **المستخدمون** في التطبيق وتفعّله وتحدد مستواه (مدير حسابات / محاسب مكتب / سكرتارية).

## 5) تفعيل الحماية الفعلية (لما تكون جاهز تمامًا)

دلوقتي أي حد معاه رابط التطبيق يقدر يستخدمه من غير تسجيل دخول أصلاً — ده عشان تقدر تكمل الاختبار والبناء من غير ما تتقفل على نفسك. لما تكون جاهز فعليًا (كل المستخدمين عندهم حسابات معتمدة):

1. من **Vercel Dashboard** ← مشروعك ← **Settings** ← **Environment Variables**.
2. ضيف متغيّر: `AUTH_ENFORCED` بقيمة `true`.
3. اعمل Redeploy.
4. بعد كده، ارجع لـ Supabase SQL Editor وشغّل `migrations/003_enable_rls_lockdown.sql` — ده اللي بيقفل قاعدة البيانات فعليًا خلف تسجيل الدخول. **اقرأ التحذير في أول الملف قبل ما تشغّله.**

---

## ملاحظات مهمة

- **صلاحيات الشاشات بالتفصيل** (مين يقدر يعدّل/يحذف في كل شاشة لوحدها) لسه مرحلة جاية — دلوقتي فيه بس 4 مستويات عامة (مالك/مدير حسابات/محاسب مكتب/سكرتارية) بيتحددوا من شاشة **المستخدمون**، والجدول اللي هيحمل التفاصيل الدقيقة (`app_user_permissions`) اتعمل بس لسه مش مفعّل في الشاشات.
- لو حصل أي مشكلة وانقفل عليك الدخول بالغلط بعد تفعيل `AUTH_ENFORCED`، رجّعها لـ `false` في Vercel وRedeploy فورًا — هيرجّع الوضع زي ما كان.
