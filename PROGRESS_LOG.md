# سجل تقدم بناء الكود — Junior Gym App

## 2026-08-13 — الدفعة الأولى

**تم إنجازه فعليًا وموجود في الكود:**
- Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind v4 + Supabase (@supabase/ssr)
- أنواع TypeScript كاملة مطابقة لقاعدة البيانات الحية (17 جدول) — انظر claude/database_schema.md
- تصميم عام بالهوية البصرية (تيل/كورال/أمبر) + خط Cairo + تخطيط RTL كامل
- شاشة Dashboard باستعلامات حية فعلية (اشتراكات نشطة، حضور اليوم، تحصيلات اليوم عبر transaction_types.name ILIKE '%استلام%'؛ رصيد الخزينة لسه placeholder لحد ما يُبنى محرك القيود)
- شاشة الطلاب (parties): عرض بالبحث، إضافة، تعديل، عرض الأدوار كـ tags

**تم التحقق منه:**
- `npx tsc --noEmit` نظيف، `npx eslint .` نظيف، `npm run build` ناجح
- تشغيل فعلي (`next start`) والتأكد إن الصفحات بترجع 200 بدون كراش. الاتصال الحي بـ Supabase من داخل بيئة العمل السحابية دي محجوب (الشبكة فيها allowlist مش شامل دومين Supabase بتاع المشروع) — ده قيد خاص ببيئة العمل بس، مش هيأثر على Vercel وقت النشر الفعلي.

**قيد تقني مهم:**
الرفع المباشر لـ GitHub من بيئة العمل السحابية دي محجوب أمنيًا ("repository not in this session's authorized repository set") حتى مع توكن صحيح من المستخدم. الحل المتبع: تسليم الكود كملف مضغوط (zip) للمستخدم، ليرفعه بنفسه عبر GitHub Desktop. خطط الجلسات القادمة على هذا الأساس من البداية (متنسيش تحاول push مباشر من الكونتينر).

**غير مؤكد ويحتاج تحقق لاحقًا:**
- القيم الفعلية لـ enum عمود party_roles.role — استخدمنا تخمين ("customer" لدور الطالب) بناءً على المتطلبات، مش مؤكد من الـ enum الحقيقي في القاعدة. لو الإدراج فشل، لازم نجيب enum_range الفعلي.

**الخطوة التالية:** المستخدم هيرفع الكود بـ GitHub Desktop، وبعدها نربط بـ Vercel وننشر، ثم نكمل باقي الشاشات (البرامج، الاشتراكات، الحضور اليومي).

---

## 2026-08-14 — الدفعة الثانية

**تحديث حالة:** الكود كان بالفعل مرفوع على GitHub (`origin/main` مطابق لـ `main` المحلي) — القيد التقني في الدفعة الأولى بخصوص حجب الرفع اتحل في جلسة سابقة ولم يُوثّق. تم أيضًا استلام `CLAUDE_PROJECT_BRIEF.md` (ملف تسليم أشمل من `PROJECT_HANDOFF.md`) ومجلد `مرفقات_مرجعية/` (schema.sql الأصلي، صور توثيق الإعداد، وملفات PDF/CSV مرجعية من الإكسيل القديم — بيانات برامج حقيقية، خريطة ذهنية للأقسام والأنشطة، إلخ).

**تأكيد حسم نقطة غير مؤكدة سابقًا:** enum عمود `party_roles.role` تم تأكيده من `schema.sql` الأصلي = `customer` / `trainer` / `employee` / `owner` / `partner` فقط (وليس فيه `accounts_manager`/`office_accountant`/`secretary` كما كان مفترضًا تخمينًا في الدفعة الأولى — دول مسميات وظيفية هتتبنى لاحقًا في نظام صلاحيات مستقل، مش قيم enum). تم تحديث `types/database.ts` و`app/students/actions.ts` و`app/students/page.tsx` (`ROLE_LABELS`) على هذا الأساس.

**تم إنجازه فعليًا وموجود في الكود:**
- شاشة المدربين (`app/trainers/*`): عرض + إضافة مدرب (`party` جديد + `party_role` بدور `trainer`) — متطلب أساسي لازم يسبق تكليف المدربين على البرامج.
- شاشة إعدادات البرامج (`app/programs/setup`): تهيئة مراكز التكلفة (الجيم/لاند) بزرار واحد (idempotent)، إضافة أقسام رئيسية، إضافة أنشطة تحت كل قسم.
- شاشة الأنشطة والبرامج (`app/programs`): عرض كل البرامج مجمّعة حسب القسم الرئيسي، مع النشاط وعدد السيشنات والقيمة والمدربين المكلّفين حاليًا ونسبهم (ونسبة الجيم المحسوبة تلقائيًا = 100% − مجموع نسب المدربين).
- شاشة إنشاء/تعديل برنامج (`app/programs/new`, `app/programs/[id]/edit`, `components/ProgramForm.tsx`, `app/programs/actions.ts`):
  - اختيار النشاط (مجمّع حسب القسم)، مركز التكلفة، عدد السيشنات أو نوع المدة (حقل نصي بـ datalist مقترحات، مش select مقفول — عشان التنوع الحقيقي في بيانات الإكسيل القديم: شهري/يومي/مسبق/نصف شهري...)، أيام وموقت التدريب، القيمة.
  - تكليف مدرب واحد أو أكتر بالنسبة لكل واحد (صفوف ديناميكية Client Component)، مع عرض حي لإجمالي نسب المدربين ونسبة الجيم المتبقية.
  - اسم البرنامج يتولّد تلقائيًا سيرفر-سايد بالقالب [النشاط] / [عدد السيشن أو نوع المدة] / [المدرب الرئيسي] زي ما هو متفق عليه في البريف.
  - عند التعديل: منطق `syncTrainerAssignments` بيقارن التكليفات الحالية (اللي `ends_at is null`) بالمُرسلة من الفورم — لو نفس المدرب بنفس النسبة يسيبها زي ما هي، لو النسبة اتغيرت أو مدرب اتشال بيقفل السجل القديم (`ends_at = اليوم`) ويفتح سجل جديد لو لزم، تطبيقًا حرفيًا لمبدأ الـ Versioned Assignments في القسم 3.2 من البريف.

**تم التحقق منه:**
- `npx tsc --noEmit` نظيف، `npx eslint .` نظيف (تحذير واحد قديم غير متعلق بالتغييرات دي بخصوص خط Cairo)، `npm run build` ناجح.
- تشغيل فعلي (`next start`) والتأكد إن كل الصفحات الجديدة (`/programs`, `/programs/new`, `/programs/setup`, `/programs/[id]/edit`, `/trainers`, `/trainers/new`) بترجع 200 من غير كراش. الاتصال الحي بـ Supabase من بيئة العمل السحابية لسه محجوب (نفس القيد المذكور في الدفعة الأولى)، فمفيش اختبار فعلي لعمليات الإدراج/التعديل حتى الآن — لازم يتعمل بعد النشر على Vercel أو من جهاز المستخدم.

**غير مؤكد ويحتاج تحقق لاحقًا:**
- سلوك `program_trainer_assignments` الفعلي عند أول استخدام حقيقي على بيانات لاند (مفيش cost_center_id افتراضي مربوط تلقائيًا بمركز تكلفة النشاط — الحقل موجود بس اختياري في الفورم حاليًا).
- لسه محتاج seed فعلي للأقسام/الأنشطة الحقيقية (راجع `مرفقات_مرجعية/الخريطة_الذهنية_لجونيور_جيم.pdf` و`البرامج_ونسب_المدربين.pdf` كمرجع للأقسام: مini junior، كورسات تعليمية، أنشطة موسمية، رياضات تخصصية، برنامج جونيور، الحالات الخاصة، حضانة، استضافة، التقييمات، التغذية).

**الخطوة التالية:** بناء شاشة إنشاء الاشتراك (`subscriptions`) مع متتبع السيشنات المرئي — طبقًا للقسم 10 بند 3 في `CLAUDE_PROJECT_BRIEF.md`.

---

## 2026-08-14 — الدفعة الثالثة (نفس اليوم، استكمال مباشر)

بعد موافقة صاحب المشروع على الاستمرار لحد الهدف النهائي بدون توقف، كملنا باقي شاشات "المرحلة 2" وبنينا محرك القيود المحاسبية الأساسي كامل.

**تم إنجازه فعليًا وموجود في الكود:**

- **شاشة إنشاء الاشتراك** (`app/subscriptions/*`, `components/SubscriptionForm.tsx`):
  - اختيار طالب (من العملاء المسجّلين) وبرنامج (مجمّع حسب القسم)، مع تعبئة تلقائية للقيمة وعدد السيشنات من البرنامج (قابلة للتعديل).
  - **Snapshot تكليف المدرب**: وقت الإنشاء بيقرا `program_trainer_assignments` النشطة (`ends_at is null`) للبرنامج ويجمّدها في `trainer_assignment_snapshot` (jsonb) — تطبيقًا حرفيًا للقسم 3.2.
  - **توليد `subscription_sessions` تلقائيًا**: دالة `generateExpectedDates` بتوزّع تواريخ السيشنات على أيام الأسبوع المحددة في `programs.session_days`، أو أسبوعيًا (نفس يوم البدء) لو البرنامج مفيهوش أيام محددة. `ends_at` بيتحسب من آخر تاريخ متوقع.
  - شاشة قائمة الاشتراكات (بالكود/اسم الطالب) وشاشة تفاصيل بمتتبع السيشنات المرئي (مربعات ملوّنة: أخضر=حضر، أحمر=غاب، أصفر=غياب غير محتسب، رمادي=لسه) — طبقًا للقسم 3.4.

- **محرك المحاسبة** (`lib/accounting.ts`) — أول نسخة شغّالة:
  - `recordTransaction()`: بتاخد اسم نوع العملية + سياق (اشتراك/عميل/برنامج/حساب نقدي...)، تسجّل صف في `daily_transactions`، وتولّد `journal_entries` تلقائيًا من `debit_sub_account_id`/`credit_sub_account_id` بتاع `transaction_types`.
  - **قاعدة COGS التلقائية**: لو النوع `triggers_cogs` ومرتبط باشتراك عليه `trainer_assignment_snapshot`، بتتولّد تلقائيًا قيود مرافقة (مدين "رواتب المدربين (COGS)" / دائن "الرواتب المستحقة - المدربين") بنسبة كل مدرب من المبلغ المحصَّل فعليًا — مش من قيمة الاشتراك الكاملة، تطبيقًا حرفيًا لقسم 4.
  - **منع الرصيد السالب للخزينة**: `getCashAccountBalance()` بتحسب رصيد أي حساب نقدي من مجموع `daily_transactions` المرتبطة بيه (مفيش FK مباشر بين `cash_accounts` و`sub_accounts`، فالحساب بيتم عن طريق `transaction_types.debit/credit_sub_account_id`). أي عملية هتخلي الرصيد سالب بترفض بخطأ واضح، تطبيقًا لقسم 7.
  - **قيد "تسوية حساب"**: النوع اتزرع بس القيد التلقائي مش مدعوم له لسه (يحتاج تحديد حسابات متغيرة يدويًا) — هيتبنى مع شاشة إيقاف/استرداد الاشتراك لاحقًا.

- **شجرة الحسابات وأنواع العمليات** (`app/accounting/setup/*`):
  - شاشة تهيئة بزراير: شجرة الحسابات (`financial_statements` ← `main_accounts` ← `sub_accounts`، أولية مبنية حرفيًا على أسماء الحسابات في قسمي 4 و5 من البريف)، الحسابات النقدية الأربعة (الخزينة/الدرج/فودافون كاش/انستا باي)، وأنواع العمليات (~45 نوع بعد فك تجميع "لكل أصل" لعملياته الفردية — القيمة التقريبية "~35" في البريف كانت قبل الفك).
  - كل الزراير idempotent (upsert بالاسم الفريد، أو فحص وجود قبل الإدراج) — تقدر تدوسها أكتر من مرة بأمان لتحديث الشجرة لاحقًا.

- **شاشة الحضور اليومي** (`app/attendance/*`, `components/AttendanceMarkForm.tsx`):
  - دخول بكود الاشتراك → عرض أقرب سيشن معلّق → تسجيل حضر/غاب (يُحتسب، بيسجّل قيد إيراد بقيمة السيشن بعد الخصم)/غياب غير محتسب (بدون قيد، لازم سبب).
  - خيار "تحصيل نقدي في نفس الوقت" بيسجّل عملية "استلام نقدي" منفصلة (تمر على فحص منع الرصيد السالب تلقائيًا لأنه استلام مش صرف، فمينفعش يتعارض، لكن أي عملية صرف تانية بعدين هتتفحص).

**فجوة schema اكتُشفت أثناء التنفيذ (محتاجة migration بسيطة):**
عمود `subscription_sessions.excused_reason` (مذكور في `schema.sql` الأصلي كحقل إجباري لسبب "غياب غير محتسب") **مش موجود فعليًا في القاعدة الحية** — أُكِّد غيابه من `DATABASE_SCHEMA.md` (استخراج مباشر من القاعدة بتاريخ 2026-08-13) ومن فشل `tsc` وقت المحاولة. الحل المؤقت المطبّق: بيتسجّل السبب كنص داخل `daily_transactions.notes` بدل عمود مخصص. **قبل الإطلاق الفعلي، شغّل في Supabase SQL Editor:**
```sql
alter table subscription_sessions add column excused_reason text;
```
وبعدها ننقل منطق الحفظ من `notes` للعمود الصحيح.

**تم التحقق منه:** `npx tsc --noEmit`، `npx eslint .`، و`npm run build` كلهم نظيفين بعد كل دفعة تعديلات. تشغيل فعلي (`next start`) وفحص كل الصفحات الجديدة (`/subscriptions`, `/subscriptions/new`, `/subscriptions/[id]`, `/accounting/setup`, `/attendance`, `/attendance?code=...`) بترجع 200 من غير كراش. **تحذير مهم:** الفحص ده كله بنية HTTP status فقط — الاتصال الحي بـ Supabase من بيئة العمل السحابية لسه محجوب، فمفيش تأكيد فعلي إن الإدراج/التحديث الحقيقي في القاعدة (خصوصًا محرك القيود ومنع الرصيد السالب) بيشتغل صح على بيانات حقيقية. **لازم اختبار فعلي بعد النشر على Vercel أو من جهاز المستخدم قبل أي استخدام إنتاجي.**

**معوقات حقيقية لسه موجودة (مش تقنية بحتة، محتاجة تدخل صاحب المشروع):**
1. **ربط Vercel**: لازم صاحب المشروع (أو حد معاه صلاحية) يسجّل دخول Vercel ويربط الـ repo — ده إجراء خارجي ما ينفعش يتعمل من هنا.
2. **RLS + نظام صلاحيات المستخدمين الحقيقي (8 أشخاص)**: يحتاج قرار استراتيجية auth (Supabase Auth الأرجح) + بيانات حقيقية (إيميلات/أدوار كل شخص) من صاحب المشروع قبل ما يتبنى.
3. **ترحيل الـ 25,000 سجل من الإكسيل القديم**: محتاج ملفات الإكسيل الفعلية (مش مرفوعة في المشروع حاليًا) + تصميم استراتيجية ترحيل ومطابقة الأرصدة الافتتاحية.
4. **عقد شركاء لاند**: لسه مرفوعش، فحساب "جاري شريك (لاند)" في شجرة الحسابات مؤقت عام وموحّد لحد ما تتحدد آلية توزيع الربح الفعلية لكل شريك.

**الخطوة التالية:** شاشة إيقاف/استرداد الاشتراك (مع قيد "تسوية حساب" التلقائي)، ثم قائمة الدخل الموحّدة والتقارير الأساسية.

---

## 2026-08-18 — الدفعة الرابعة

**تم إنجازه فعليًا وموجود في الكود:**

- **شاشة إيقاف/استرداد الاشتراك** (`app/subscriptions/[id]/stop/*`, `components/StopSubscriptionForm.tsx`):
  - بتحسب تلقائيًا عدد السيشنات غير المستهلكة والقيمة الإجمالية (قبل الخصم) والصافية (بعد الخصم).
  - بتنفّذ "إيقاف اشتراك" (أثر تدقيقي بدون قيد) و"استرداد الاشتراك" (قيد حقيقي مدين العملاء/دائن النقدية، بيمر على فحص منع الرصيد السالب).
  - **قيد "تسوية حساب" التلقائي بالتفصيل معمول عمدًا كخطوة يدوية** (فورم بسيط لاختيار حساب مدين/دائن والقيمة) بدل ما يتخمّن — لأن آلية ترحيله لحسابات محددة **مذكورة صراحة في قسم 9 من البريف كبند مؤجل محتاج قرار من صاحب المشروع**، مش حاجة واضحة كفاية عشان تتبنى تلقائيًا بثقة. القيمة المقترحة بتتحسب وتتعرض جاهزة، بس التنفيذ الفعلي قرار بشري.

- **تصدير/استيراد إكسيل لكل شاشات البيانات الأساسية** (`lib/excel.ts` + `components/ExcelImportForm.tsx` + `app/{students,trainers,programs,subscriptions}/{export/route.ts,import/page.tsx}`):
  - مكتبة `exceljs` (**مش** `xlsx`/SheetJS — نسخة الـ npm بتاعتها فيها ثغرتين أمنيتين عاليتين (Prototype Pollution + ReDoS) بدون إصلاح متاح، ومباشرة في مسار قراءة ملفات مرفوعة من المستخدم، فاستبدلناها).
  - كل شاشة فيها زرار "تصدير إكسيل" (يرجع نفس البيانات المعروضة كملف xlsx بأسماء أعمدة عربية) و"استيراد إكسيل" (فورم رفع ملف، بيرجّع تقرير نجاح/فشل تفصيلي صف بصف).
  - استيراد البرامج والاشتراكات بيعمل "find or create" تلقائي للأقسام/الأنشطة/المدربين/العملاء بالاسم المطابق تمامًا لو مش موجودين — **خطر تكرار لو الاسم مكتوب بشكل مختلف شوية، لازم مراجعة يدوية بعد أي استيراد كبير** (هيفيد وقت ترحيل الـ 25 ألف سجل لاحقًا، بس محتاج حذر).

- **نظام تسجيل الدخول والمستخدمين — الإصدار الأول** (migration + auth pages):
  - `migrations/002_auth_and_permissions.sql`: جدولين جداد `app_users` (مربوط بـ `auth.users`، فيه `role`: owner/accounts_manager/office_accountant/secretary) و`app_user_permissions` (جاهز لصلاحيات تفصيلية لكل شاشة، **مش مفعّل في الكود لسه** — الخطوة الجاية).
  - `migrations/003_enable_rls_lockdown.sql`: ملف منفصل (تشغيله اختياري ومؤجل عمدًا) بيقفل كل الجداول الأساسية خلف تسجيل دخول فعلي — فيه تحذير واضح في الأول قبل التشغيل.
  - `/signup`, `/login`, `/pending`, `/users` (Owner فقط) — أول حساب يتسجّل في النظام بيبقى Owner ومفعّل تلقائيًا (bootstrap)، أي حساب بعده بيفضل معلّق لحد الاعتماد.
  - `proxy.ts` (اسمه الجديد بدل `middleware.ts` — Next.js 16 غيّر التسمية، شوف قسم "قيد تقني" تحت) بيتحقق من تسجيل الدخول بس لو `AUTH_ENFORCED=true` في متغيرات البيئة — **مطفي افتراضيًا عمدًا** عشان صاحب المشروع يقدر يكمل يختبر النظام من غير ما يتقفل على نفسه. خطوات التفعيل الكاملة موثّقة في `SETUP_AUTH.md` الجديد.
  - صلاحيات الشاشات/التعديل/الحذف بالتفصيل لكل يوزر (بند "المرحلة 3" التاني في `خطة_التنفيذ.md`) **لسه مبنية جزئيًا بس** — فيه بس 4 مستويات عامة دلوقتي، مش تحكم شاشة بشاشة.

**قيد تقني اتحل أثناء التنفيذ:** Next.js 16 غيّرت اسم ملف الـ middleware لـ `proxy.ts` (والدالة لازم تتسمى `proxy` مش `middleware`) — الاسم القديم كان بيشتغل لكن بيطلع تحذير deprecation في الـ build. راجعنا `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` (طبقًا لتعليمات `AGENTS.md`) وطبّقنا التسمية الجديدة من البداية.

**تم التحقق منه:** `npx tsc --noEmit`، `npx eslint .`، `npm run build` كلهم نظيفين. تشغيل فعلي وفحص كل الصفحات الجديدة (`/login`, `/signup`, `/pending`, `/users`, `/subscriptions/[id]/stop`, وكل روابط `/export`+`/import`) بترجع 200. **نفس التحذير المتكرر:** الاتصال الحي بـ Supabase محجوب من بيئة العمل دي، فمفيش تأكيد فعلي إن الـ migrations أو تدفق تسجيل الدخول شغّالين على قاعدة بيانات حقيقية — **لازم اختبار فعلي بعد تطبيق `migrations/002_auth_and_permissions.sql` والنشر على Vercel.**

**الخطوة التالية:** قائمة الدخل الموحّدة والتقارير الأساسية (كشف حساب العميل/المدرب، تقرير النقدية، ميزان المراجعة والمركز المالي، الالتزامات المستحقة) — قسم "المرحلة 4" في `خطة_التنفيذ.md`.

---

## 2026-08-19 — الدفعة الخامسة (المرحلة ٤ كاملة)

**تم إنجازه فعليًا وموجود في الكود** — كل شاشات `app/reports/*`، مبنية فوق دالة مشتركة جديدة `getSubAccountBalances()` في `lib/accounting.ts` (رصيد كل حساب فرعي = مجموع مدين−دائن من `journal_entries`، بفلترة اختيارية بالتاريخ ومركز التكلفة):

- **كشف حساب العملاء** (`/reports/customers`, `/reports/customers/[id]`): رصيدان منفصلان تطبيقًا للقسم 3.1 — **تجاري** (إجمالي الاشتراكات المسجّلة ناقص المحصّل نقدًا، **تقريبي عمدًا**: لسه مش بيعالج تأثير إيقاف/استرداد اشتراك على المديونية بدقة) و**محاسبي** (رصيد حساب "العملاء" الفعلي من القيود). + تفصيلي بالنشاط.
- **كشف حساب المدربين والموظفين** (`/reports/staff`, `/reports/staff/[id]`): بيجمع قيود COGS التلقائية (`trainer_party_id`) مع أي عمليات راتب/مكافأة يدوية (`daily_transactions.employee_party_id`) في رصيد واحد لحساب "الرواتب المستحقة". **ملاحظة تفسير schema:** عمود `employee_party_id` في `daily_transactions` كان تعليقه في `schema.sql` "الموظف اللي سجّل العملية" (يوحي إنه توثيق مين أدخل البيانات) — فسّرناه بدل كده كـ"الموظف/المدرب اللي العملية خاصة بيه" (بالقياس على `customer_party_id`)، لأن من غيره مفيش عمود تاني يربط عمليات الرواتب بصاحبها. **محتاج تأكيد من صاحب المشروع.** الشاشة نفسها فيها فورم تسجيل مباشر (مكافأة/خصومات إدارية/تسليم راتب) بيمر على فحص منع الرصيد السالب.
- **النقدية والخزينة** (`/reports/cash`): رصيد كل حساب نقدي (بيستخدم `getCashAccountBalance` الموجودة بالفعل) + كشف حركة تفصيلي لأي حساب بالضغط عليه.
- **قائمة الدخل الموحّدة** (`/reports/income-statement`): التحكمات الثلاثة المطلوبة بالظبط (Group By قسم▸نشاط▸برنامج، فترة من/إلى، نطاق مركز تكلفة) طبقًا للقسم 8. **تبسيط موثّق داخل الشاشة نفسها:** بند "إيجار" في الصيغة الأصلية مفروض يتخصم منفصل قبل "مجمل الربح" وبعدين يتوزّع حسب مركز التكلفة، لكن عمليًا هو مصروف عام مش مرتبط ببرنامج بعينه (زي باقي المصروفات غير المباشرة بالظبط)، فتم إدماجه في نفس مجمّع "المصروفات الموزّعة بنسبة الإيراد" بدل ما يتفصل كخطوة منفصلة — النتيجة الرقمية النهائية (صافي الربح) صحيحة، بس العرض المرحلي (مجمل الربح) مش مطابق حرفيًا للترتيب في البريف.
- **ميزان المراجعة** (`/reports/trial-balance`) و**المركز المالي** (`/reports/balance-sheet`): كل حساب فرعي مبوّب تحت حسابه الرئيسي وقائمته المالية، بفلتر "حتى تاريخ" اختياري. المركز المالي بيعرض تحذير لو الأصول ≠ الالتزامات+حقوق الملكية (متوقع حاليًا لحد ما تتسجّل الأرصدة الافتتاحية).
- **الالتزامات المستحقة** (`/reports/liabilities`): إجمالي أعلى الشاشة وتفصيل كل بند تحته، تطبيقًا حرفيًا لوصف قسم 9.

**قيد تقني (React Compiler / eslint-plugin-react-hooks الجديد):** الإصدار المستخدم هنا بيرفض إعادة تعيين متغيّر (`let x; x += ...`) جوه أي دالة render — حتى لو مش hook فعلي. اتصلّح في كل شاشات التقارير باستخدام `.reduce()` بترجع accumulator جديد بدل ما تعدّل متغيّر خارجي، بدل الحلقات العادية (`for`) مع متغيّر تراكمي.

**تم التحقق منه:** `npx tsc --noEmit`، `npx eslint .`، `npm run build` نظيفين. تشغيل فعلي وفحص كل شاشات `/reports/*` بترجع 200. **نفس التحذير المتكرر:** الأرقام لسه مش متأكدة على بيانات حقيقية لأن الاتصال بـ Supabase محجوب من بيئة العمل دي — **لازم اختبار فعلي بعد النشر.**

**معوقات/نقاط تحتاج قرار من صاحب المشروع (مجمّعة، جاهزة للمراجعة سوا):**
1. تأكيد تفسير عمود `daily_transactions.employee_party_id` (شرح فوق).
2. دقّة الرصيد "التجاري" لكشف حساب العميل بعد إيقاف/استرداد اشتراك.
3. قرار نهائي لآلية توزيع "الإيجار" في قائمة الدخل (مفصول عن المصروفات الموزّعة، أو مدمج زي ما هو دلوقتي).
4. باقي المعوقات الخارجية من الدفعات السابقة (Vercel، RLS الفعلي، ملفات الترحيل، عقد شركاء لاند) لسه قائمة.

**الخطوة التالية:** لا يوجد بند تقني إضافي واضح من "المرحلة 4" — الباقي كله خطوات خارجية (المرحلة 5: الترحيل) أو قرارات محتاجة صاحب المشروع (المرحلة 6 + النقاط المجمّعة فوق).

---

## 2026-08-19 (تابع) — الشاشة الرئيسية بتصميم dashboard-mockup.html

بناءً على طلب صريح من صاحب المشروع: `app/page.tsx` اتبنى من جديد بالكامل ليطابق تصميم `dashboard-mockup.html` المعتمد — شبكة أيقونات وصول للموديولات (زي Odoo) بدل كروت الإحصائيات الأربعة اللي كانت موجودة (اتشالت نهائيًا زي ما اتطلب، مش منقولة لمكان تاني).

- 4 مجموعات زي المعتمد بالظبط: العمليات اليومية، الإعدادات الأساسية، المالية والتقارير، الإدارة — بنفس الأيقونات والألوان الدوّارة (تيل/كورال/أمبر) من التصميم الأصلي.
- كل تايل بيوصل للشاشة الحقيقية المبنية فعليًا (`/attendance`, `/subscriptions/new`, `/students`, `/reports/cash`... إلخ).
- **التايلات اللي مفيش لها شاشة مبنية بعد** (الموظفون كشاشة إدارة مستقلة، شركاء لاند، الأصول الثابتة) اتسابت ظاهرة بنفس تصميم المخطط الأصلي بس **معطّلة بصريًا** (باهتة، مش قابلة للضغط، وعليها "قريبًا") بدل ما تتشال أو تودّي لرابط غلط — عشان الشاشة تفضل خريطة كاملة لكل الموديولات المخطط ليها حتى لو مش مبنية لسه.
- شكل الـ AppShell (السايدبار الجانبي + معلومات المستخدم/الخروج) اتسابت زي ما هي من غير تغيير — التصميم الجديد اتطبّق على محتوى الشاشة الرئيسية بس، مش على هيكل التنقل العام اللي بقى معتمد في كل الشاشات التانية.

**تم التحقق منه:** `tsc`/`eslint`/`build` نظيفين، وفحص فعلي للـ HTML المُترجم يوضح كل النصوص والتايلات ظاهرة صح.